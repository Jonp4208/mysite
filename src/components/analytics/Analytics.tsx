'use client';

import PageViewTracker from './PageViewTracker';

const Analytics = () => {
  // This component now just includes the PageViewTracker
  return <PageViewTracker />;
};

export default Analytics;
